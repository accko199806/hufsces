package hufs.ces.closure

class Entry {

    FiveCards fiveCards
    List transform

    Entry(FiveCards fiveCards, List transform) {
        this.fiveCards = fiveCards
        this.transform = transform
    }
}
